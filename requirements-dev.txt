# Test-only dependencies. Kept out of requirements.txt so they stay out of the
# production image (Dockerfile installs requirements.txt only).
#
#   python3 -m venv venv
#   ./venv/bin/pip install -r requirements-dev.txt
#   ./venv/bin/pytest
#
# The suite covers the wallet money path, which needs no browser: SQLAlchemy,
# FastAPI and Celery are enough, so Playwright is deliberately absent.
-r requirements.txt
pytest==8.3.4
